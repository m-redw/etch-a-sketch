# etch-a-sketch

Simple browser version of an etch-a-sketch.

Hover over the grid to start coloring.

Each square starts at 0% opacity, so repeatedly leave and enter your mouse onto the square to get it to full opacity.

Colors are also randomized.

If you want to change the canvas size, press the button at the top and simply enter a number (1 - 100).